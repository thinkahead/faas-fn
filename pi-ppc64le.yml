version: 1.0
provider:
  name: openfaas
  gateway: http://gateway-external-openfaas.apps.ibm-hcs.priv
functions:
  pi-ppc64le:
    lang: dockerfile
    handler: ./pi-ppc64le
    image: karve/pi-ppc64le:latest
    environment:
      read_timeout: "5s"
      write_timeout: "5s"
      exec_timeout: "5s"
